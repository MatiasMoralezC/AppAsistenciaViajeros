const main = async () => {
  const buttonLading = document.getElementById('button-landing')
  buttonLading.addEventListener('click', () => window.location = "./../pages/anuncios.html")
}
document.addEventListener("DOMContentLoaded", main)
